Download and run the script:

> ATTENTION! This script expect a CLEAN system with /mnt already mounted with /boot as EFI partition

```bash
curl -fsSLO https://raw.githubusercontent.com/Mr-EmPee/My-Arch/master/init.sh
```